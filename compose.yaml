services:
  producer:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: gtfsrt-cts
    env_file: ".env"
    environment:
      - TZ=Europe/Paris
    ports:
      - "127.0.0.1:50004:3000"
    restart: unless-stopped